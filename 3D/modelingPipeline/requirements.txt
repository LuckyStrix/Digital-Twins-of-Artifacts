# Core pipeline dependencies
open3d
numpy<2.5           # open3d / onnxruntime have no wheels built against numpy 2.5+ yet
rembg[gpu]          # background removal; use rembg (no [gpu]) on CPU-only machines
onnxruntime-gpu<1.27  # rembg GPU backend; 1.27+ requires a newer cuDNN/CUDA combo than we install
torch               # required by rembg's birefnet-general model
pillow              # photo gallery in the GUI

# Install with python3, e.g.:  python3 -m pip install -r requirements.txt
#
# rembg model: birefnet-general is the default; others download on first use
# System dependencies (install via apt, not pip):
#   colmap
#   exiftool
#   cudnn9-cuda-12  (see ../README.md for the NVIDIA repo + GPG key steps)
